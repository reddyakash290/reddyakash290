pipeline {
    agent any

    stages {
        stage('run python script') {
            steps {
                echo 'above script'
                sh 'python3 hello.py'
            }
        }
    }
}
